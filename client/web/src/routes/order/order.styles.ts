import {css} from '@microsoft/fast-element';
import {mixinScreen} from '../../styles';

export const OrderStyles = css`
  .quantity-ok {
    color: green;
  }    
  
  .quantity-nok {
    color: red;
  }
`;
